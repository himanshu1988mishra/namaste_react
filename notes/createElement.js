/*

   1. React is a JS library for building user interfaces. It allows developers to create reusable UI components.
      ReactDOM is JS library which is used to render React components to the DOM. It handles browser DOM operations.

   2. Elements created using React.createElement() are react elements not HTML elements, those are actually objects. 
      when ReactDOM renders reactRoot then ReactDOM creates HTML elements from those react elements and adds them to 
      the browser DOM.

*/
